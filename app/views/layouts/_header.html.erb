<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <%= button_tag(type: 'button', class: 'navbar-toggle collapsed', data: { toggle: 'collapse', target: 'navbar-collapse' }, aria: { expanded: false }) do %>
                <%= content_tag(:span, nil, class: 'sr-only') %>
                <%= content_tag(:span, nil, class: 'icon-bar') %>
                <%= content_tag(:span, nil, class: 'icon-bar') %>
                <%= content_tag(:span, nil, class: 'icon-bar') %>
            <% end %>
            <%= link_to 'Sample Saas Product', '#', class: 'navbar-brand' %>
        </div>
        <div class="collapse navbar-collapse" id="navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <% if current_user %>
                <li class="dropdown">
                    <%= link_to('#', class: 'dropdown-toggle', role: 'button', aria: { haspopup: true, expanded: false }, data: { toggle: 'dropdown' }) do %>
                        <%= current_user.name %> <span class="caret"></span>
                    <% end %>
                    <ul class="dropdown-menu">
                        <li><%= link_to 'Ver Conta', user_path(current_user) %></li>
                        <li role="separator" class="divider"></li>
                        <li><%= link_to 'Sair', logout_path %></li>
                    </ul>
                </li>
                <% else %>
                <li>
                    <%= link_to 'Entrar', login_path %>
                </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
